def main():
    primes = [2]
    for isPrime in range(2,10000):
        for divider in primes:
            if(isPrime%divider == 0):
                break
        else:
            primes.append(isPrime)
    print(primes)

if __name__ == '__main__':
    main()
